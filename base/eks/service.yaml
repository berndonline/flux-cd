---
apiVersion: v1
kind: Service
metadata:
  name: eks
  namespace: default
  annotations:
    fluxcd.io/automated: "true"
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: eks
